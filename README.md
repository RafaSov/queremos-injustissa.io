# queremos-injustissa.io

Landing page da guild **Queremos Injustiça** — World of Warcraft.

## Desenvolvimento

```bash
# Instalar dependências
npm install

# Compilar TypeScript
npm run build

# Servir localmente (http://localhost:3000)
npm run serve
```

## Estrutura

- `index.html` — Estrutura da página (Hero, Quem somos, Objetivo, Contato)
- `styles.css` — Estilos e animações
- `main.ts` / `main.js` — Animações de scroll, partículas, parallax
